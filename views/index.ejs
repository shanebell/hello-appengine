<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="minimum-scale=1, initial-scale=1, width=device-width"/>
    <meta name="description" content="Hello App Engine"/>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap"/>
    <title>Hello World</title>
    <style>
        body {
            font-family: Roboto, sans-serif;
            font-size: 18px;
            margin: auto;
            margin: auto;
            width: 1200px;
            padding-top: 20px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        h1 {
            display: flex;
            justify-content: center;
            font-size: 28px;
            font-weight: 300;
            padding-bottom: 10px;
        }

        form {
            padding-bottom: 20px;
            display: flex;
            justify-content: center;
        }

        input {
            padding: 8px;
            font-size: 16px;
            margin: 4px;
        }

        table {
            border-collapse: collapse;
        }

        th, td {
            padding: 10px;
            text-align: left;
            border: solid 1px #ccc;
        }

        th {
            background-color: #f0f0f0;
        }

        td {
            font-weight: 300;
        }
    </style>
</head>
<body>

<h1>Hello Serverless.  Goodbye Meetings</h1>

<form method="get" action="/form">
    <input type="text" name="text" placeholder="Type some text for me to store" required size="50" maxlength="100">
    <input type="submit" value="Submit">
</form>

<table>
    <thead>
    <tr>
        <th>Date</th>
        <th>Method</th>
        <th>Path</th>
        <th>Query parameters</th>
    </tr>
    </thead>
    <tbody>
    <% requests.forEach((request) => { %>
        <tr>
            <td><%= request.date %></td>
            <td><%= request.method %></td>
            <td><%= request.path %></td>
            <td>
                <%= request.query.map((query) => `${query.name}=${query.value}`).join("&") %>
            </td>
        </tr>
    <% }); %>
    </tbody>
</table>
</body>
</html>
